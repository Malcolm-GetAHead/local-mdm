# Browser Test Playbook — Local MDM Dashboard

Instructions for running browser-based tests against the Local MDM dashboard.
Each section is a test suite. Each `- [ ]` item is an executable test step.

**Base URL:** `http://localhost:8080`

**Run:** `node run-playbook.js` (headless) or `node run-playbook.js --headed` (watch)

---

<!-- Populate these sections during Sprint 5d as each page is built. -->
<!-- See run-playbook.js for supported DSL: Visit, Fill, Click, Verify, Wait, Select, Navigate -->
